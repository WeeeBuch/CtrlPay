<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:CtrlPay.Avalonia"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1280" d:DesignHeight="720"
             x:Class="CtrlPay.Avalonia.MainView"
             xmlns:vm="using:CtrlPay.Avalonia.ViewModels"
             x:DataType="vm:MainViewModel"
             Background="{DynamicResource Color.Background}">
  
    <Grid ColumnDefinitions="Auto, *">
        <local:NavbarView Grid.Column="0" Margin="0"/>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled">
            <ContentControl Grid.Column="1" 
                        Content="{Binding CurrentPage}" 
                        Margin="20">
            </ContentControl>
        </ScrollViewer>
    </Grid>
</UserControl>
